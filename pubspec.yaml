name: firebaselogin
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # -------------------------------
  # 🔥 Firebase Required Packages
  # -------------------------------
  firebase_core: ^3.5.0
  firebase_auth: ^5.7.0   # Required for passwordless email link login
  shared_preferences: ^2.5.3
  uni_links: ^0.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
